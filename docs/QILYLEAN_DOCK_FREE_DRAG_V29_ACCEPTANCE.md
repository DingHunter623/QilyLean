# QilyLean 悬浮功能区 V29 验收｜2026-08-19

本轮整改只接受以下结果：

- PC：悬浮功能区可沿 X/Y 两轴自由拖动，不再只允许上下移动。
- Mobile：触摸拖动与 PC 同一逻辑，按钮仍可正常点击。
- 拖动距离超过 7px 视为拖动，不误触按钮动作；短按仍执行原按钮功能。
- 拖动后位置写入 `qilyDockPositionV2`，刷新后恢复。
- 位置按视口边界约束，不允许整体拖出可视区域；窗口尺寸变化后继续约束。
- 旧 `qilyDockTop` 单轴位置可迁移，成功保存 V2 后清理旧键。
- 电脑统一 62×62、15px；手机统一 58×58、14px。
- `分享 / 官网` 两行必须与其它悬浮按钮使用同级字号，禁止“官网”单独缩小到 10/11/12px。
- 首页、知识页、代表项目、信任中心、项目合作、友情链接以及所有公开 HTML 使用相同 Dock V2 与 Navigation V29 cache-bust。
- 箭头 Geometry V4 与 2026-08-14 场景简图02对称箭头不得回退。
