User-agent: *
Disallow: /

# PRELAUNCH LOCK
# Keep blocked until ICP number is displayed, HTTPS is valid,
# production networking is verified, content compliance passes,
# and the public-release checklist is complete.
