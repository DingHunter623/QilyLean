name: Retired six-core pricing validator — manual only

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: |
          echo "Six-core pricing architecture is no longer the public business hierarchy."
          echo "Pricing may still cover digital/APP/web capabilities, but they must not be labeled as core business peers."
