name: DEPRECATED - legacy 3+3 taxonomy vocabulary writer

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: R2 retirement notice
        run: |
          echo 'Deprecated by QilyLean R2 on 2026-08-12.'
          echo 'The 3+3 business taxonomy is retired. Six core capabilities are the only current business taxonomy.'
          echo 'Use scripts/enforce-six-core-static-source.js and the R2 six-core validation workflow instead.'
