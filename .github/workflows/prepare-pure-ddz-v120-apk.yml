name: Pure DDZ v1.2 APK Sync - Manual Only

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hold:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Android APK website publishing is intentionally held.'
          echo 'Run a new release/sync workflow only after explicit user approval of the web version.'
