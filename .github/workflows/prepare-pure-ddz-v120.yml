name: Pure DDZ v1.2 Visual Shell - Legacy Manual Only

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hold:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Legacy v1.2 shell writer disabled for automatic PR execution.'
          echo 'Current web iteration is maintained by the webfix flow.'
