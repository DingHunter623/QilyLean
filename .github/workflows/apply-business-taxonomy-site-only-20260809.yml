name: DEPRECATED - legacy 3+3 site taxonomy writer

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: R2 retirement notice
        run: |
          echo 'Deprecated by QilyLean R2 on 2026-08-12.'
          echo 'Do not rematerialize the retired 3+3 taxonomy.'
          echo 'Current source of truth: six core capabilities + manufacturing-operations asset system.'
