name: DEPRECATED - legacy 3+3 taxonomy and trust writer

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: R2 retirement notice
        run: |
          echo 'Deprecated by QilyLean R2 on 2026-08-12.'
          echo 'Trust and evidence remain active, but the old workflow must not rewrite current six-core taxonomy.'
          echo 'Use current trust pages plus the R2 static-source and evidence rules.'
