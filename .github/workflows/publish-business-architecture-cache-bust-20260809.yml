name: DEPRECATED - legacy 3+3 business cache writer

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: R2 retirement notice
        run: |
          echo 'Deprecated by QilyLean R2 on 2026-08-12.'
          echo 'The former 20260809 3+3 runtime/cache chain must not be republished.'
          echo 'Current runtime baseline is R2 static-first + Fast Native Navigation V5.'
