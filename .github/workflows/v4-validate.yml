name: QilyLean V4 Validate

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
    paths:
      - '**/*.html'
      - '**/*.css'
      - '**/*.js'
      - '**/*.json'
      - 'sitemap*.xml'
      - 'robots.txt'
      - 'scripts/**'
      - 'data/**'
      - 'meta/**'
      - '.github/workflows/**'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: qily-v4-validate-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Syntax check V4 governance tools
        run: |
          node --check scripts/site-system-v4-validate.js
          node --check scripts/site-system-v4-repository-audit.js
          node --check scripts/site-system-v4-workflow-audit.js
          node --check scripts/site-system-v4-sitemap-lastmod.js
          node --check scripts/site-system-v4-build-meta.js
          node --check scripts/site-system-v4-public-verify.js

      - name: G1 source and SSOT validation
        run: node scripts/site-system-v4-validate.js

      - name: Existing SEO gate
        run: npm run test:seo

      - name: Existing R6 visual and interaction gate
        run: npm run test:visual

      - name: Audit workflow migration surface
        run: node scripts/site-system-v4-workflow-audit.js

      - name: Audit sitemap lastmod drift
        run: node scripts/site-system-v4-sitemap-lastmod.js

      - name: Audit repository weight and historical blobs
        run: node scripts/site-system-v4-repository-audit.js

      - name: Validate generated reports
        run: |
          test -s maintenance/site-system-v4-workflow-audit.json
          test -s maintenance/site-system-v4-sitemap-audit.json
          test -s maintenance/site-system-v4-repository-audit.json
          git diff --check

      - name: Upload V4 audit evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: qily-v4-audit-evidence
          path: |
            maintenance/site-system-v4-workflow-audit.json
            maintenance/site-system-v4-sitemap-audit.json
            maintenance/site-system-v4-repository-audit.json
          if-no-files-found: warn
          retention-days: 14
