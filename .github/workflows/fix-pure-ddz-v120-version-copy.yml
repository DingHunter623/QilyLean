name: Pure DDZ v1.2 Version Copy - Legacy Manual Only

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hold:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Legacy automatic version-copy writer disabled.'
          echo 'Do not republish Android download surfaces without explicit user approval.'
