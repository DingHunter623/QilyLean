name: Site regression audit — manual only

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Confirm retired six-core self-heal is not executed
        run: |
          echo "Automatic six-core self-heal retired on 2026-08-18."
          echo "Current business hierarchy is maintained by static source and the business-architecture validator."
